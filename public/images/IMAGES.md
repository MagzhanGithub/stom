# Images Required

Replace each placeholder below with the real photo from the client.
All images should be exported as `.jpg` (AVIF/WebP auto-generated by Next.js).
Recommended minimum dimensions noted next to each file.

## Root `/public/images/`

| File | Dimensions | Usage |
|------|-----------|-------|
| `hero.jpg` | 900×1100 px | Hero section, patient photo (right column) |
| `doctor.jpg` | 800×1000 px | Doctor section portrait |
| `clinic-og.jpg` | 1200×630 px | OpenGraph / social share preview |

## `/public/images/before-after/`

| File | Dimensions | Usage |
|------|-----------|-------|
| `whitening-before.jpg` | 800×600 px | Before/after slider — whitening |
| `whitening-after.jpg`  | 800×600 px | |
| `veneers-before.jpg`   | 800×600 px | Before/after slider — veneers |
| `veneers-after.jpg`    | 800×600 px | |
| `implant-before.jpg`   | 800×600 px | Before/after slider — implant |
| `implant-after.jpg`    | 800×600 px | |

## Notes
- Optimize with [Squoosh](https://squoosh.app) before adding (target < 200 KB each)
- All `alt` text is already written in Russian inside the components
- `blurDataURL` placeholders are base64 tiny JPEGs — replace them once real images are added
  (generate with: `npx plaiceholder <image-path>`)
